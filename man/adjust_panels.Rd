% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panels.R
\name{adjust_panels}
\alias{adjust_panels}
\title{Adjust the positions of reference panels on a thermal image}
\usage{
adjust_panels(
  panels,
  target_raster,
  black_value,
  gray_value,
  white_value,
  avg_value,
  rounding_precision = 5,
  theta1 = 0,
  htrans1 = 0,
  vtrans1 = 0,
  theta_range = 3,
  theta_length = 3,
  htrans_range = 5,
  htrans_length = 5,
  vtrans_range = 5,
  vtrans_length = 5,
  min_overlap = 100,
  fact = 1,
  cores = 1,
  min_cor = 0.5,
  maxiter = 3,
  method = "Nelder-Mead",
  reltol = 10^-5,
  trace = 1
)
}
\arguments{
\item{panels}{An sf object of polygons representing reference panel coordinates.}

\item{target_raster}{A SpatRaster object representing a thermal image to which the
coordinates of the reference panels should be aligned.}

\item{black_value}{A numeric of length one. The value that thermal pixels corresponding
to the black panel should be given.}

\item{gray_value}{Similar to black_value, but for the gray panel.}

\item{white_value}{Similar to black_value, but for the white panel.}

\item{avg_value}{A numeric of length one. The value given to background pixels that
do not correspond to any panel.}

\item{rounding_precision}{An single integer indicating the decimal to round the
transformed coordinates to before converting to polygons. This is to prevent
small numerical differences from resulting in apparently open polygons}

\item{theta1}{A numeric. The initial value for theta.}

\item{htrans1}{A numeric. The initial value for htrans.}

\item{vtrans1}{A numeric. The initial value for vtrans.}

\item{theta_range}{A numeric. The ± range of values to explore around
theta1 should a call to find_initial be necessary.}

\item{theta_length}{A numeric. The number of values of theta to test initially
if a call to find_initial is required.}

\item{htrans_range}{Similar to theta_range for the htrans parameter.}

\item{htrans_length}{Similar to theta_length for the htrans parameter.}

\item{vtrans_range}{Similar to theta_range for the vtrans parameter}

\item{vtrans_length}{Similar to theta_length for the vtrans parameter.}

\item{min_overlap}{A numeric value of length one. The minimum number of values
that must be compared between two rasters for the correlation to be considered
valid. Otherwise, a correlation of 0 is returned.}

\item{fact}{A numeric. The factor to use to reduce the resolution of the
rasters to speed up the estimation of the best parameters in find_initial.
Defaults to 1 (no aggregation is performed).}

\item{min_cor}{A numeric. The minimum correlation that must be obtained
from initial guesses to go on with optim.}

\item{maxiter}{An integer. The maximum number of iterations of find_initial
to go through. The number of parameter combinations texted increases by
8 times for each iteration, so this number should be kept low otherwise
computation time will become extremely long.}

\item{method}{A character. The method to use for optimization, passed to optim.}

\item{reltol}{A numeric. The relative tolerance for convergence in optim.}

\item{trace}{A numeric value passed to optim to determine the level of
output verbosity.}
}
\value{
An sf polygon object similar to the input one, but with updated
coordinates corresponding to the optimized panel positions.
}
\description{
This function aims to enable the use of \code{\link{optimize_transform}} for
finding the transformation of reference panel coordinates that best
matches the thermal image that they are associated with. The idea is to
create a raster that will best correlate to the thermal image when the panel
positions are aligned with their actual position with the image. To achieve this,
higher values should be attributed to the black and gray panels, and lower values
to the white panel. This rasterized form of the panel coordinates is then passed
to \code{\link{optimize_transform}} to find the rotation/translation parameters
that best align the panel coordinates to the target thermal image.
}
\examples{
NULL
}
