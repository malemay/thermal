% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap.R
\name{compute_overlaps}
\alias{compute_overlaps}
\title{Compute and add overlap transform parameters to flight metadata}
\usage{
compute_overlaps(
  metadata,
  theta_guess = TRUE,
  theta1 = NULL,
  htrans1 = 0,
  vtrans1 = 0,
  theta_range = 5,
  theta_length = 9,
  htrans_range = 20,
  htrans_length = 5,
  vtrans_range = 20,
  vtrans_length = 5,
  min_overlap = 100,
  fact = 1,
  cores = 1,
  min_cor = 0.8,
  maxiter = 2,
  method = "Nelder-Mead",
  reltol = 10^-5,
  verbose = TRUE,
  trace = verbose
)
}
\arguments{
\item{metadata}{A data.frame of metadata on a flight, such as read by
\code{\link{read_metadata}}.}

\item{theta_guess}{A logical value indicating whether initial guesses for
theta (transform rotation angle) should be guessed from the metadata. This
requires the column 'GimbalYawDegree' (which specifies the yaw angle of the
gimbal when the picture was taken) to be found in the metadata. If
theta_guess is FALSE, then the initial guess is equal to the value of
theta1, if not NULL. If theta_guess is FALSE and theta1 is NULL, then theta1
is set to 0 with a warning.}

\item{theta1}{A numeric. The initial value for theta.}

\item{htrans1}{A numeric. The initial value for htrans.}

\item{vtrans1}{A numeric. The initial value for vtrans.}

\item{theta_range}{A numeric. The ± range of values to explore around
theta1.}

\item{theta_length}{A numeric. The number of values of theta to test initially.}

\item{htrans_range}{Similar to theta_range for the htrans parameter.}

\item{htrans_length}{Similar to theta_length for the htrans parameter.}

\item{vtrans_range}{Similar to theta_range for the vtrans parameter}

\item{vtrans_length}{Similar to theta_length for the vtrans parameter.}

\item{min_overlap}{A numeric value of length one. The minimum number of values
that must be compared between two rasters for the correlation to be considered
valid. Otherwise, a correlation of 0 is returned.}

\item{fact}{A numeric. The factor to use to reduce the resolution of the
rasters to speed up the brute-force search for the best parameters.
Defaults to 1 (no aggregation is performed).}

\item{cores}{The number of cores to use for parallel computing. Defaults to 1.}

\item{min_cor}{A numeric. The minimum correlation that must be obtained
from initial guesses to go on with optim.}

\item{maxiter}{An integer. The maximum number of brute-force iterations of
to go through. The number of parameter combinations tested increases by 8
times for each iteration, so this number should be kept low otherwise
computation time will become extremely long.}

\item{method}{A character. The method to use for optimization, passed to optim.}

\item{reltol}{A numeric. The relative tolerance for convergence in optim.}

\item{verbose}{A logical value. If TRUE, progress on the iterations
in the search for parameters will be output to the console.}

\item{trace}{A numeric value passed to optim to determine the level of
output verbosity. By default, it is set to the same value as verbose
(1 if TRUE and 0 if FALSE), but this parameter allows to control it
separately.}
}
\value{
A metadata data.frame similar to the input one, but with added
columns "theta", "htrans", "vtrans" and "corr" related to the coordinate
transformation.
}
\description{
This function computes transform parameters for all images in a dataset to
allow overlap correction in downstream analyses. These parameters, as well
as the resulting correlation between successive images, are added as columns
to the input metadata. This function is basically a convenient wrapper for
the workhorse function \code{\link{optimize_transform}} and the helper
\code{\link{add_tparams}}. Users who want to understand how these functions
work or gain finer control over the analysis should read their respective
documentation.
}
\examples{
NULL
}
