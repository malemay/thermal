% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap.R
\name{optimize_transform}
\alias{optimize_transform}
\title{Optimize the transformation for a raster to match its target}
\usage{
optimize_transform(
  x,
  y,
  theta1 = 0,
  htrans1 = 0,
  vtrans1 = 0,
  theta_range = 5,
  theta_length = 9,
  htrans_range = 20,
  htrans_length = 5,
  vtrans_range = 20,
  vtrans_length = 5,
  min_overlap = 100,
  fact = 1,
  cores = 1,
  min_cor = 0.8,
  maxiter = 2,
  method = "Nelder-Mead",
  reltol = 10^-5,
  verbose = TRUE,
  trace = verbose
)
}
\arguments{
\item{x}{The target raster.}

\item{y}{The raster to transform.}

\item{theta1}{A numeric. The initial value for theta.}

\item{htrans1}{A numeric. The initial value for htrans.}

\item{vtrans1}{A numeric. The initial value for vtrans.}

\item{theta_range}{A numeric. The ± range of values to explore around
theta1.}

\item{theta_length}{A numeric. The number of values of theta to test initially.}

\item{htrans_range}{Similar to theta_range for the htrans parameter.}

\item{htrans_length}{Similar to theta_length for the htrans parameter.}

\item{vtrans_range}{Similar to theta_range for the vtrans parameter}

\item{vtrans_length}{Similar to theta_length for the vtrans parameter.}

\item{min_overlap}{A numeric value of length one. The minimum number of
values that must be compared between two rasters for the correlation to be
considered valid. Otherwise, a correlation of 0 is returned.}

\item{fact}{A numeric. The factor to use to reduce the resolution of the
rasters to speed up the brute-force search for the best parameters.
Defaults to 1 (no aggregation is performed).}

\item{cores}{The number of cores to use for parallel computing. Defaults to
1.}

\item{min_cor}{A numeric. The minimum correlation that must be obtained from
initial guesses to go on with refining the estimates using optim.}

\item{maxiter}{An integer. The maximum number of brute-force iterations to
go through. The number of parameter combinations tested increases by 8 times
for each iteration, so this number should be kept low otherwise computation
time will become extremely long.}

\item{method}{A character. The method to use for optimization, passed to optim.}

\item{reltol}{A numeric. The relative tolerance for convergence in optim.}

\item{verbose}{A logical value. If TRUE, progress on the iterations in the
search for parameters will be output to the console.}

\item{trace}{A numeric value passed to optim to determine the level of
output verbosity. By default, it is set to the same value as verbose (1 if
TRUE and 0 if FALSE), but this parameter allows to control it separately.}
}
\value{
The result of running the optim function on the transformation of y
to the target raster x.
}
\description{
Optimize the transformation for a raster to match its target
}
\examples{
# We use the second and third images in the test dataset
image1 <- terra::rast(system.file("extdata/DJI_0671_thermal.tiff", 
                                  package = "thermal"),
                      noflip = TRUE)
image2 <- terra::rast(system.file("extdata/DJI_0677_thermal.tiff",
                                  package = "thermal"),
                      noflip = TRUE)

# Compute the transformation parameters to match image1 from image2
tparams <- optimize_transform(x = image1, y = image2,
                              fact = 2, min_cor = 0.6,
                              maxiter = 2, reltol = 10^-2,
                              verbose = TRUE)

# Running check_transform to verify the accuracy
check_transform(x = image1, y = image2, params = tparams)

# Setting min_cor higher will result in more parameters tested by brute-force approach
# before control is passed to optim
tparams2 <- optimize_transform(x = image1, y = image2,
                               fact = 2, min_cor = 0.8,
                               maxiter = 2, reltol = 10^-2,
                               verbose = TRUE)

# In this case, the results obtained are sill the same
identical(tparams$optim$par, tparams2$optim$par)

# Since the transformation involves very little rotation
# and horizontal translation, we can restrict the range
# of theta and htrans values tested to speed up
tparams3 <- optimize_transform(x = image1, y = image2,
                               theta_length = 1, htrans_length = 3,
                               fact = 2, min_cor = 0.8,
                               maxiter = 2, reltol = 10^-2,
                               verbose = TRUE)
}
