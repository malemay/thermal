% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_metadata}
\alias{plot_metadata}
\title{Plots the location of pictures from flight metadata}
\usage{
plot_metadata(
  metadata,
  coords = c("GPSLongitude", "GPSLatitude"),
  base_crs = NULL,
  new_crs = NULL,
  color = "black",
  color_palette = "RdBu",
  n_colors = 11,
  round_digits = 0,
  features = NULL,
  feature_color = "black",
  arrows = FALSE,
  arrow_angle = 30,
  arrow_length = grid::unit(0.25, "inches"),
  title = NULL,
  cex.axis = 1,
  cex.points = 1
)
}
\arguments{
\item{metadata}{A data.frame of metadata on a flight, such as returned by
\code{\link{read_metadata}}.  Must minimally contain columns GPSLongitude and
GPSLatitude for longitude and latitude coordinates in the WGS84 datum.}

\item{coords}{A character vector of length two indicating which columns
in metadata are to be interpreted as X and Y coordinates (in that order).}

\item{base_crs}{An object that can be interpreted as a CRS description. Will
be used to set the coordinate system of the metadata. If NULL (the default),
then WGS84 (EPSG:4326) is assumed.}

\item{new_crs}{A coordinate reference system (such as returned by
\code{\link[sf]{st_crs}} to project the points to before display. If NULL
(the default), then data is displayed in the original coordinate system.}

\item{color}{A vector of character, numeric or factor data to specify how
to color the points. If a character value, this is interpreted as a color
value. If a factor, when a distinct color is used (from a predetermined set)
to represent each value of the factor. If a numeric, then a color scale
(see the color_scale argument) is used to translate numeric values to colors.}

\item{color_palette}{A character specifying the color palette to use when
mapping numeric values onto a color scale. Must correspond to a color
palette in the RColorBrewer package. See
\code{\link[RColorBrewer]{brewer.pal.info}} for more details.}

\item{n_colors}{The number of colors to use in the color scale when mapping
numeric values to colors. See \code{\link[RColorBrewer]{brewer.pal.info}}
for more details.}

\item{round_digits}{The decimal position to round the numeric values to
when mapping numeric values to a color scale.}

\item{features}{An optional sf object representing the position of a feature
of interest, whose coordinates will be transformed if new_crs is not NULL. Will
be coerced to an object that can be plotted using ­\code{\link[sf]{st_geometry}}
and \code{\link[sf]{st_as_grob}}.}

\item{feature_color}{A single character value: the color to use for the
features.}

\item{arrows}{A logical value specifying whether arrows should be plotted
to show the trajectory of the drone.}

\item{arrow_angle}{A numeric value controlling the angle of the arrows.
See \code{\link[grid]{arrow}}.}

\item{arrow_length}{A unit object controlling the length of the arrows.
See \code{\link[grid]{arrow}}.}

\item{title}{A single character string. The title of the plot.}

\item{cex.axis}{A character expansion value applied to the axis labels.}

\item{cex.points}{A character expansion value applied to the points.}
}
\value{
NULL, invisibly. This function is invoked for its plotting
side-effect.
}
\description{
This function can be used to get a first glance of the layout of a flight.
The color parameter can be used to specify different colors for points
depending on characteristics of the metadata.
}
\examples{
NULL
}
